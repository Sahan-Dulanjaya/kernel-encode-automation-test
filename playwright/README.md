# Kernel Encode API Automation Test – Playwright 🚀

This project automates API tests using Playwright (JavaScript).

## Covered Tests
- 🔐 Login (`POST /api/login`)
- 📦 Product List (`GET /api/products`)
- ➕ Create Product (`POST /api/products`)

## Setup & Run

```bash
cd kernel-encode-automation-test/playwright
npm install
npx playwright install
npx playwright test